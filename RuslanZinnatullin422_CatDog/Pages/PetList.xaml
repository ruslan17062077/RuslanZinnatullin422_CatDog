<Page x:Class="RuslanZinnatullin422_CatDog.Pages.PetList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:RuslanZinnatullin422_CatDog.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PetList">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="23*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <StackPanel Orientation="Horizontal">
                <TextBlock Margin="10 10 0 0 ">Поиск</TextBlock>
                <TextBox MinWidth="100" Margin="10 " Name="poisk" TextChanged="poisk_TextChanged"></TextBox>
            </StackPanel>
            <Button Margin="50 5 0 5" Click="Button_Click">Добавить</Button>
        </StackPanel>
        <StackPanel Grid.Row="1">

            <DataGrid x:Name="PsinaData">
                <DataGrid.Columns>
                    <DataGridTextColumn Binding="{Binding img}"></DataGridTextColumn>
                    <DataGridTextColumn Binding="{Binding Name}"></DataGridTextColumn>
                    <DataGridTextColumn Binding="{Binding Description}"></DataGridTextColumn>
                    
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>

    </Grid>
</Page>
